version: 2.1
jobs:
  build:
    docker:
        - image: cimg/node:17.7.2
    steps:
      - checkout
      - run: ls
      - run: npm install
      - run: npm run test
      - run: npm run cov
      - store_artifacts:
          path: docs
          
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
